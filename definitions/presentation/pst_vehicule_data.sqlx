config {
  type: "table",
  schema: "presentation_iveco",
  name: "pst_vehicle_data",
  tags: ["daily"]
}

SELECT
    vin,
    tripStartDateTime,
    tripEndDateTime,
    odometerStart,
    odometerEnd,
    totalEngineHours,
    engineTotalHoursAtEnd

FROM ${ref('gld_vehicle_data')}